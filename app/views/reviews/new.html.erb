
<div class="max-w-md mx-auto mt-8">
  <div class="bg-white rounded-lg border border-gray-200 p-6">
    <h2 class="text-xl font-semibold text-gray-800 mb-4">
      Donner mon avis pour <%= @restaurant.name %>
    </h2>

    <%= form_with model: [@restaurant, @review], class: "space-y-4" do |f| %>
      <div>
        <%= f.label :rating, "Note (0-5)", class: "block text-sm font-medium text-gray-700 mb-1" %>
        <%= f.number_field :rating,
              min: 0, max: 5,
              class: "w-full px-3 py-2 border border-gray-300 rounded focus:outline-none focus:ring-2 focus:ring-blue-500",
              placeholder: "Entre 0 et 5" %>
      </div>

      <div>
        <%= f.label :content, "Commentaire", class: "block text-sm font-medium text-gray-700 mb-1" %>
        <%= f.text_area :content,
              rows: 4,
              class: "w-full px-3 py-2 border border-gray-300 rounded focus:outline-none focus:ring-2 focus:ring-blue-500",
              placeholder: "Partagez votre expérience..." %>
      </div>

      <div class="flex space-x-3">
        <%= f.submit "Envoyer mon avis",
              class: "flex-1 bg-blue-600 text-white py-2 rounded hover:bg-blue-700 transition cursor-pointer" %>

        <%= link_to "Annuler", restaurant_path(@restaurant),
              class: "flex-1 bg-gray-300 text-gray-700 py-2 rounded hover:bg-gray-400 transition text-center" %>
      </div>
    <% end %>
  </div>
</div>
