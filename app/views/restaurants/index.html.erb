
<div class="max-w-2xl mx-auto mt-8">
  <div class="bg-white rounded-lg border border-gray-200 p-6">
    <h1 class="text-2xl font-bold text-gray-800 mb-6">Liste des Restaurants</h1>

    <div class="space-y-3">
      <% @restaurants.each do |restaurant| %>
        <div class="flex items-center justify-between py-3 border-b border-gray-100 last:border-b-0">
          <div>
            <%= link_to restaurant.name, restaurant_path(restaurant),
                class: "text-lg font-medium text-gray-800 hover:text-blue-600" %>
            <p class="text-sm text-gray-500 capitalize"><%= restaurant.category %></p>
          </div>
          <span class="text-gray-400">→</span>
        </div>
      <% end %>
    </div>

    <div class="mt-6">
      <%= link_to "Ajouter un restaurant", new_restaurant_path,
          class: "block w-full bg-blue-600 text-white text-center py-2 rounded hover:bg-blue-700 transition" %>
    </div>
  </div>
</div>
    <!-- ✅ -->
